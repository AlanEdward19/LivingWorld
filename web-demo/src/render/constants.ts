/** Escala de tile do renderer Canvas/WebGL do Settlement View (AD-020) — independente da escala
 * do mapa-múndi SVG (`IsoProjection.TILE_WIDTH/HEIGHT`), são dois renderers diferentes agora. */
export const TILE = 56;
